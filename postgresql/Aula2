NSERT (Criar)
INSERT INTO usuarios (nome, email, ativo)
VALUES ('João', 'joao@email.com', true);

SELECT (Ler)
SELECT * FROM usuarios;

SELECT nome, email FROM usuarios;

SELECT * FROM usuarios WHERE ativo = true

UPDATE (Atualizar)
UPDATE usuarios
SET ativo = false
WHERE id = 1;

DELETE (Excluir)
DELETE FROM usuarios
WHERE id = 1;


⚠️ No mercado: DELETE quase sempre tem WHERE.

rdenação e filtros
SELECT * FROM usuarios
ORDER BY criado_em DESC;

SELECT * FROM usuarios
WHERE nome LIKE 'J%';
